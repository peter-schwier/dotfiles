#! /bin/sh

{{ template "script_test.sh" "~/.config/sh/env.sh" }}

# Set the default umask in case it isn't set elsewhere.
umask 022

if which vim >/dev/null; then
    export VISUAL="vim"
    export EDITOR="vim"
fi
if which vi >/dev/null; then
    export VISUAL="vi"
    export EDITOR="vi"
fi

export ENV="$HOME/.shenv"